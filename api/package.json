{
  "name": "api",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "nodemon --exec 'node --experimental-specifier-resolution=node src/app.js'",
    "docker:build": "docker-compose build",
    "docker:start": "docker-compose up",
    "docker:dev": "npm run docker:build && npm run docker:start",
    "docker": "docker-compose up --build -d"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
