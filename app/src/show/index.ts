export enum ERoute {
  login = "/login",
  home = "/home",
  menu = "/menu",
  reward = "/reward",
  register = "/register",  
  movieRegister = "/movieregister",  
  movieDetail = "/moviedetail",  
  chat = "/chat",
}

const route = ERoute;
export default {
  route,
};
